# 2026-05-13 工作记录

## Timesheet 功能审计与优化建议
- 全面审计了 timesheet 模块的前后端代码
- 梳理了数据库表结构、API 路由、前端交互逻辑
- 识别了 7 大优化方向、20+ 具体优化点
- 关键发现：
  - 缺少数据库唯一索引（resource_id+project_id+date 可能重复）
  - 保存时全量传输所有输入框数据，未做增量更新
  - 无审批流程（status 字段只有 draft）
  - 每格无备注字段（notes 在表中有但前端未暴露）
  - 移动端表格体验差（固定 60px 宽度）
  - 缺少日工时上限校验
  - 复制上周/批量填充等效率功能缺失

## Timesheet 体验升级（已完成并推送 GitHub）
- 提交: 2197f15 feat(timesheets): 全面体验升级
- 增量保存：对比 `_initialValues` 仅提交修改项，减少无效请求
- 单元格备注：Bootstrap Modal 编辑，小图标提示有备注的单元格
- 键盘导航：↑↓←→ 和 Enter 在表格网格间移动焦点
- 防抖自动保存：停止输入 1.5s 后写入 localStorage（键: `ts_draft_{resourceId}_{weekStart}`）
- 预设工时按钮：聚焦时浮现 2h/4h/6h/8h 快捷按钮（mousedown + preventDefault 避免 blur 冲突）
- 数据库：迁移脚本去重后创建 `idx_timesheets_unique` 唯一索引
- API：batch 保存支持 `notes` 和 `status` 字段
- i18n：新增 `timesheets.add_notes`, `timesheets.draft_saved`, `timesheets.draft_restored`, `timesheets.no_changes`
- 更新了本地 `dist/*.min.js` 和 `style.min.css`

## 部署记录
- 时间: 2026-05-13 17:21
- 提交: 2197f15
- 数据库备份: db/resource-guru-backup-20260513_172148.db
- 服务状态: crewboard online (pid=1062562), port=3000, HTTP 200 OK
